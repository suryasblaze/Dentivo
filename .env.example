# Short bill links (optional). Without these the app still works — the bill
# travels inside the link instead, which makes the link long.
# Create a Supabase project, run docs/shortlink.sql, then fill these in here
# and in Vercel → Settings → Environment Variables.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
